const { spawn } = require('child_process')
const fs = require('fs')
const path = require('path')

module.exports = class DApp {
  constructor (params) {
    this._params = params
  }

  start () {

  }
}
